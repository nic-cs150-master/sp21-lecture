#include <iostream>
using namespace std;

int main()
{
    int length = 0, width = 0;

    cout << "Enter the length of the rectangle as integer: ";
    cin >> length;

    cout << "Enter the width of the rectangle as integer: ";
    cin >> width;

    // cout << "Enter the length and width of the rectangle as integers: ";
    // cin >> length >> width;

    cout << "Length = " << length << ", Width = " << width << '\n';

    return 0;
}